# 🤖 Nado Telegram Trading Bot

Повністю автономний торговий бот, який слухає сигнали з Telegram і виконує угоди на Nado (nado.xyz) з автоматичним управлінням TP/SL.

## ⚡ Особливості

- **Автономна торгівля**: Виконує угоди автоматично на основі сигналів з Telegram каналу
- **Mean Reversion Strategy**: 
  - SHORT_SQUEEZE → Відкриває SHORT позицію
  - LONG_FLUSH → Відкриває LONG позицію
- **Управління ризиками**: Конфігурований ризик на угоду, кредитне плече, відсотки TP/SL
- **Офіційний Nado SDK**: Використовує офіційний TypeScript SDK для підписання та виконання
- **WebSocket моніторинг**: Відстеження заповнення ордерів в реальному часі через Nado SDK
- **Telegram сповіщення**: Повний lifecycle оновлень угод
- **Trading Hours**: Опціональні часові обмеження торгівлі
- **Symbol Whitelist**: Торгує тільки дозволеними символами
- **Production Ready**: Сумісність з Railway/VPS, graceful shutdown, auto-reconnect

## 🆕 Що нового в цій версії

### ✅ Інтеграція з офіційним Nado SDK
- Використовується `@nadohq/client` для всіх операцій
- Видалено ручне криптографічне підписання
- Видалено власну WebSocket імплементацію
- SDK обробляє EIP-712 підписання, order formatting, fills

### ✅ Виправлено критичні помилки
- Node.js syntax error виправлено
- Telegram polling працює коректно
- Видалено зайве логування
- Оптимізовано використання пам'яті

## 📋 Передумови

1. **Nado Account**
   - Фінансований subaccount з ≥ $5 USDT0
   - Торгівля увімкнена на Arbitrum

2. **Telegram**
   - Bot token від [@BotFather](https://t.me/botfather)
   - Channel ID де публікуються сигнали
   - Chat ID для отримання сповіщень

3. **Wallet**
   - Private key з правами підписання (ніколи не ділиться фактичними коштами)

## 🚀 Встановлення

### Локальна розробка

```bash
# Клонувати або завантажити проект
cd nado-telegram-bot

# Встановити залежності
npm install

# Скопіювати шаблон оточення
cp .env.example .env

# Відредагувати .env зі своїми credentials
nano .env

# Запустити бота
npm start
```

### Railway Deployment

1. **Push до GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin <your-repo-url>
   git push -u origin main
   ```

2. **Deploy на Railway**
   - Перейти на [railway.app](https://railway.app)
   - Натиснути "New Project" → "Deploy from GitHub"
   - Вибрати свій репозиторій
   - Додати environment variables в Railway dashboard
   - Deploy!

3. **Environment Variables в Railway**
   Додати всі змінні з `.env.example` в розділі Variables Railway.

## ⚙️ Конфігурація

### Environment Variables

| Variable | Description | Default | Required |
|----------|-------------|---------|----------|
| `PRIVATE_KEY` | Wallet private key (0x...) | - | ✅ |
| `TELEGRAM_BOT_TOKEN` | Telegram bot token | - | ✅ |
| `TELEGRAM_CHANNEL_ID` | Channel to listen for signals | - | ✅ |
| `TELEGRAM_NOTIFY_CHAT_ID` | Chat for notifications | - | ✅ |
| `NADO_NETWORK` | `mainnet` or `testnet` | `mainnet` | ❌ |
| `RISK_PERCENT` | Risk per trade (% of balance) | `2.5` | ❌ |
| `TAKE_PROFIT_PERCENT` | TP distance from entry | `0.8` | ❌ |
| `STOP_LOSS_PERCENT` | SL distance from entry | `0.3` | ❌ |
| `LEVERAGE` | Trading leverage | `20` | ❌ |
| `MAX_DAILY_TRADES` | Maximum trades per day | `5` | ❌ |
| `MAX_OPEN_POSITIONS` | Max concurrent positions | `1` | ❌ |
| `TRADING_HOURS_ENABLED` | Enable time filtering | `true` | ❌ |
| `TRADING_START_UTC` | Start time (HH:MM) | `05:00` | ❌ |
| `TRADING_END_UTC` | End time (HH:MM) | `14:00` | ❌ |
| `ALLOWED_SYMBOLS` | Comma-separated symbols | `BTCUSDT,ETHUSDT,ADAUSDT` | ❌ |

### Приклад управління ризиками

```env
RISK_PERCENT=2.5      # Використовувати 2.5% балансу на угоду
LEVERAGE=20           # З 20x кредитним плечем
TAKE_PROFIT_PERCENT=0.8   # Вихід на +0.8% прибутку
STOP_LOSS_PERCENT=0.3     # Вихід на -0.3% збитку
```

**Розрахунок розміру позиції:**
```
Balance: $1000
Risk: 2.5% = $25
With 20x leverage: $500 position size
```

## 📡 Формат Telegram сигналу

Бот очікує повідомлення в такому форматі:

```
🚨 SIGNAL DETECTED
Symbol: ADAUSDT
Type: SHORT_SQUEEZE
Direction: LONG
```

```json
{
  "symbol": "ADAUSDT",
  "signalType": "SHORT_SQUEEZE",
  "direction": "LONG",
  "stats": {
    "lastPrice": 0.2967
  }
}
```

**Логіка сигналу (Mean Reversion):**
- `SHORT_SQUEEZE` → Відкриває **SHORT** позицію
- `LONG_FLUSH` → Відкриває **LONG** позицію

⚠️ Поле `direction` **ІГНОРУЄТЬСЯ** ботом.

## 🛠️ Як це працює

### Архітектура

```
┌─────────────────┐
│  Telegram       │
│  Channel        │──┐
└─────────────────┘  │
                     │  1. Signal
┌─────────────────┐  │     Received
│  Trading Bot    │◄─┘
│  (Node.js)      │
└────────┬────────┘
         │
         │  2. Parse & Validate
         │     ↓
         │  3. Check:
         │     • Whitelist
         │     • Trading hours
         │     • Position limits
         │     ↓
         │  4. Execute Trade
         ↓
┌─────────────────┐
│  Nado SDK       │
│  (@nadohq/      │
│   client)       │
└────────┬────────┘
         │
         │  5. Place Orders:
         │     • Market entry
         │     • TP limit
         │     • SL limit
         │
         ↓
┌─────────────────┐
│  WebSocket      │
│  (SDK managed)  │──┐
└─────────────────┘  │
                     │  6. Order Filled
┌─────────────────┐  │
│  Telegram       │◄─┘     Notification
│  Notifications  │
└─────────────────┘
```

### Підписання замовлень (SDK керує)

Nado вимагає криптографічні підписи для всіх операцій запису:

- Офіційний SDK обробляє EIP-712 підписання
- Private key залишається на вашому сервері
- Підписи є криптографічним доказом наміру
- Не можуть бути повторно використані
- Операції читання не вимагають підписання

## 🚨 Критичні правила

### НЕ РОБИТИ:
- ❌ Ділитися своїм private key
- ❌ Зберігати ключі в коді або version control
- ❌ Використовувати один wallet для кількох ботів
- ❌ Торгувати поза trading hours (якщо увімкнено)
- ❌ Перевищувати max daily trades

### ЗАВЖДИ:
- ✅ Тримати ≥ $5 USDT0 в subaccount
- ✅ Використовувати environment variables для secrets
- ✅ Моніторити сповіщення
- ✅ Тестувати з малими розмірами позицій спочатку
- ✅ Тримати бота оновленим

## 📚 Ресурси

- [Nado Documentation](https://docs.nado.xyz)
- [Nado TypeScript SDK](https://docs.nado.xyz/developer-resources/typescript-sdk)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Railway Docs](https://docs.railway.app)

## 🆘 Troubleshooting

### Бот не запускається
- Перевірити `.env` файл існує і правильно налаштований
- Перевірити формат private key (має починатися з 0x)
- Переконатися Node.js ≥ 18

### Не виконуються угоди
- Перевірити symbol є в `ALLOWED_SYMBOLS`
- Перевірити trading hours активні
- Підтвердити balance ≥ $5 USDT0
- Перевірити daily trade limit не перевищено

### WebSocket відключається
- SDK автоматично перепідключається
- Перевірити стабільність мережі
- Перевірити статус Nado API

## ⚖️ Disclaimer

**Цей бот торгує справжніми грошима. Використовуйте на свій ризик.**

- Немає гарантії прибутку
- Минула продуктивність ≠ майбутні результати
- Торгівля криптовалютою несе значний ризик
- Тестуйте з малими сумами спочатку
- Ніколи не інвестуйте більше, ніж можете дозволити собі втратити

## 📄 License

MIT License - See LICENSE file for details

---

**Питання?** Перевірте [Nado Discord](https://discord.gg/nado) або [docs.nado.xyz](https://docs.nado.xyz)
